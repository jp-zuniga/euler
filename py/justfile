set quiet

[private]
default:
  just --list --list-heading $'  `euler-py` recipes:\n' --list-prefix "    "

# ---------------------------------------------------------

fix:
  ruff check --fix --unsafe-fixes

fmt:
  ruff format

lint:
  ruff check --unsafe-fixes
